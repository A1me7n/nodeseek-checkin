# 在别的机器上跑 NodeSeek 签到

## 需要带过去的三样东西

| 东西 | 说明 |
|---|---|
| `checkin.py` | 脚本本体（单文件，纯标准库就能签到） |
| `.nsk_session.json` | **登录态**（cookie）—— 没有它签不了，等于账号密码 |
| 一条 cron | 每天跑一次 |

`install.sh` 负责把这三样装好。

**可选第四样**：`/root/.nsk-ch-profile`（25M，浏览器 profile）。
带上它，新机器上的**过盾 / 重新登录**流程也能直接用（里面存着浏览器侧的 session + cf_clearance）。
不带也行 —— 签到本身不受影响，只是哪天被 Cloudflare 拦了，要在新机器上做一次
`--login`（要你在手机上收验证码）。

## 方式 A：一键安装（推荐）

**1. 把安装包传到新机器**（在你本机执行）：

```bash
ssh root@新机器 'mkdir -p /root/nsk-deploy'
scp -r /root/nsk/deploy/* root@新机器:/root/nsk-deploy/
# 登录态单独拷（凭据文件，别用微信/邮件明文传）
scp /root/.nsk_session.json root@新机器:/root/nsk-deploy/.nsk_session.json
# 可选：浏览器 profile（想过盾/重新登录也直接可用就带上）
# scp -r /root/.nsk-ch-profile root@新机器:/root/
```

**2. 在新机器上安装**：

```bash
ssh root@新机器
cd /root/nsk-deploy
bash install.sh                  # 只装签到（零额外依赖，够用）
# bash install.sh --with-browser # 再加 playwright+xvfb，支持过盾/重新登录
```

装完会自动跑一次 `--status` 自检，输出 `✅ 登录态正常` 就算成功。

## 方式 B：手动三步

```bash
# 1) 脚本（python3 标准库，不需要 pip）
mkdir -p /root/nsk && cp checkin.py /root/nsk/ && chmod 755 /root/nsk/checkin.py

# 2) 登录态
cp .nsk_session.json /root/.nsk_session.json && chmod 600 /root/.nsk_session.json

# 3) cron —— 北京时间 08:00 换算到本机时区
#    本机是 UTC → 00:00 ；IST → 05:30 ；CEST → 02:00 ；CST(北京) → 08:00
crontab -e     # 加一行：
# 30 5 * * * /usr/bin/python3 /root/nsk/checkin.py >> /root/nsk/cron.out 2>&1
```

`install.sh` 会按机器的实际时区自动算，不用你心算。

## 两台机器一起跑会不会冲突？

不会。签到接口是幂等的：已经签过会返回 `今天已完成签到，请勿重复操作`，
脚本当成正常（`☑️`，退出码 0），不会报错、也不会重复领。

## 常见问题

**Q: 一定要 python3 吗？能不能更简单？**
签到本身只要 python3 标准库（不用装任何 pip 包）。`--with-browser` 才需要 playwright。

**Q: 不带浏览器会怎样？**
正常签到没问题。只有两种情况需要浏览器：
① Cloudflare 把签到接口拦了（换 IP 后概率变高）→ 要浏览器过盾刷 cf_clearance
② 登录态失效 → 要浏览器走邮箱验证码重新登录
如果你的机器经常换 IP，建议加 `--with-browser`。

**Q: 登录态多久失效？**
不确定（本机这份已经用了 5 天+）。失效时输出 `⚠️ 登录态已失效`，这时：
让用户本人在手机上进 NodeSeek →「邮箱动态验证」→ 点「发送验证码」，
把邮件里的码发来，然后 `checkin.py --login <码>`（需要浏览器版本）。

**Q: 签到时间想改？**
改 cron 那一行即可。`30 5 * * *` = IST 05:30 = 北京 08:00。

**Q: 怎么确认在跑？**
`tail /root/nsk/checkin.log`（每次运行都会追加一行 JSON），
或 `cat /root/nsk/cron.out`（cron 的 stdout/stderr）。
